# EMC 与 LAMMPS 中文百科全书：项目规范

## 1. 项目名称

**中文名称：** EMC 与 LAMMPS 中文百科全书  
**英文名称：** EMC & LAMMPS Chinese Encyclopedia  
**项目目录：** `EMC_LAMMPS_Chinese_Encyclopedia`

本项目是非官方中文翻译、注释与实践参考项目，仅用于个人学习、实验室内部使用和科研查阅。

---

## 2. 项目目标

本项目旨在将 EMC（Enhanced Monte Carlo）与 LAMMPS 的官方英文资料翻译、整理并构建为可在本地浏览的中文技术百科。

项目不只是对英文文档进行机械翻译，还需要建立面向中文使用者的结构化知识体系，包括：

1. EMC 官方手册的系统中文翻译；
2. EMC Setup 命令、语法和文件格式参考；
3. EMC 分子、聚合物及复杂体系建模教程；
4. EMC 支持的力场与 LAMMPS 力场样式映射；
5. LAMMPS 用户指南的中文翻译与解释；
6. 常用 LAMMPS 命令的中文参考；
7. LAMMPS 输入脚本和数据文件格式说明；
8. EMC 到 LAMMPS 的完整模拟工作流；
9. 能量最小化、升温、NVT、NPT和生产模拟流程；
10. 并行计算、HPC、轨迹可视化和模拟数据分析；
11. 常见错误、排查方法和版本差异；
12. 中英文术语表；
13. 命令、任务、关键词、错误和文件格式索引；
14. 官方来源、版本信息与参考文献；
15. 可以通过 MkDocs Material 构建的本地中文网站。

---

## 3. 项目边界

### 3.1 项目包含的内容

项目主要处理：

- EMC 官方手册；
- EMC 官方网站及发行包内资料；
- EMC Setup 脚本和相关命令；
- EMC 官方示例；
- LAMMPS 官方文档；
- LAMMPS 官方示例和发布说明；
- EMC 与 LAMMPS 之间的文件、单位和力场映射；
- 用户经过实际验证的模拟流程；
- 为初学者编写的必要物理解释和操作说明。

### 3.2 项目暂不包含的内容

除非用户明确扩大范围，当前项目不包括：

- 通用网页抓取平台；
- 通用网页自动翻译系统；
- React、Vue、FastAPI 等独立前后端系统；
- 在线用户账号和权限系统；
- 云端数据库；
- 自动绕过登录、验证码或付费墙；
- 未经授权的大规模内容重新发布；
- 与 EMC、LAMMPS 无关的软件文档；
- 没有官方资料或实验依据支持的功能说明。

### 3.3 技术栈边界

项目继续使用：

- Markdown；
- MkDocs；
- MkDocs Material；
- Python 检查和索引脚本；
- pytest；
- Git。

除非存在明确且经过记录的技术理由，不得无理由重写为其他框架。

---

## 4. 目标读者

主要读者包括：

1. 初次学习 EMC 和 LAMMPS 的物理、材料、化学专业学生；
2. 使用 EMC 构建分子体系的科研人员；
3. 使用 LAMMPS 开展分子动力学模拟的初学者；
4. 需要查询命令、参数、文件格式和错误信息的用户；
5. 需要从 EMC 构建体系并转入 LAMMPS 模拟的研究人员。

内容应兼顾技术准确性与初学者可读性，但不得为了简化而改变物理含义或软件语义。

---

## 5. 目标版本

项目当前涉及以下软件版本：

| 组件 | 版本 | 用途 |
|---|---|---|
| EMC | 9.4.4 | EMC 文档与建模的主要目标版本 |
| EMC Setup | 5.3 | EMC Setup 脚本主要参考版本 |
| LAMMPS（服务器） | 7 Feb 2024 Update 1 | Ubuntu 服务器验证版本 |
| LAMMPS（本地） | 22 Jul 2025 Update 4 | macOS 本地验证版本 |
| MkDocs | 以项目依赖记录为准 | 文档网站构建 |
| MkDocs Material | 以项目依赖记录为准 | 网站主题 |

必须明确区分：

- 文档目标版本；
- 官方在线文档版本；
- 服务器验证版本；
- 本地验证版本；
- 示例实际运行版本。

不得将不同版本的语法、默认值、限制和运行结果混合描述。

如果某个命令或功能存在版本差异，页面必须增加“版本说明”部分。

---

## 6. 信息来源

### 6.1 来源优先级

项目内容按照以下顺序选择依据：

1. 项目中保存的 EMC 官方手册；
2. EMC 官方网站和官方发行包；
3. LAMMPS 官方文档；
4. LAMMPS 官方源码、示例和发布说明；
5. EMC 或 LAMMPS 官方引用的论文；
6. 软件作者或维护者发布的正式说明；
7. 经过验证的用户实践记录；
8. 其他资料只能作为辅助说明。

博客、论坛、问答网站和 AI 模型记忆不能代替官方资料。

### 6.2 来源记录

来源统一记录在：

```text
sources/source_manifest.csv
````

每个来源至少应记录：

* 来源类型；
* 名称；
* 软件版本；
* 本地路径；
* 官方 URL；
* 获取状态；
* 获取或核对日期；
* 备注。

### 6.3 页面来源

重要页面应包含：

```markdown
## 官方来源

- 官方标题：
- 官方章节或命令：
- 官方 URL：
- 本地来源：
- 适用版本：
- 核对日期：
```

对于本地 PDF 或文本，应记录可复查的章节、页码或行号范围。

---

## 7. 内容分类

项目内容分为以下四类，必须明确区分。

### 7.1 官方文档

对官方内容的忠实翻译或准确转述：

```markdown
> **官方文档：** 以下内容根据官方资料翻译或整理。
```

### 7.2 编者注释

为中文读者增加的背景、物理解释或使用建议：

```markdown
> **编者注：** 以下内容为补充解释，不属于官方原文。
```

### 7.3 本机验证

只有在实际环境中执行并保存证据后才能使用：

```markdown
> **本机验证：** 以下结果已在指定软件版本和环境中运行验证。
```

验证记录至少应包含：

* 操作系统；
* 软件版本；
* 执行命令；
* 输入文件；
* 退出状态；
* 关键输出；
* 验证日期。

### 7.4 待验证

尚未实际运行，或者缺少充分证据的内容：

```markdown
> **待验证：** 以下内容根据官方语法或理论分析整理，尚未在本项目环境中实际运行。
```

禁止将 AI 推断、代码静态检查或“看起来合理”标记为本机验证。

---

## 8. 翻译规范

### 8.1 基本原则

翻译应：

* 准确；
* 完整；
* 正式；
* 可读；
* 术语统一；
* 保留原文限制条件；
* 保持技术含义不变。

### 8.2 不翻译的内容

以下内容通常保持英文原样：

* EMC 和 LAMMPS 命令；
* 关键字；
* 参数名；
* 变量名；
* 命令 ID；
* 文件名；
* 文件扩展名；
* 代码；
* 命令行；
* 路径；
* 公式；
* 输入和输出中的固定标识。

### 8.3 专业术语

专业术语首次出现时采用：

```text
中文译名（English term）
```

后续使用统一中文译名。尚未确定的译法应加入术语表并标记为待核对。

### 8.4 限制性表达

必须准确翻译：

* `must`
* `must not`
* `required`
* `optional`
* `default`
* `deprecated`
* `only`
* `cannot`
* `may`
* `recommended`
* `warning`

不得省略默认值、适用条件、单位、异常情况或版本限制。

### 8.5 代码和公式

* 代码块内容不得翻译；
* 公式不得改变变量和数学结构；
* 公式后的文字说明可以翻译；
* 注释可以翻译，但不得修改命令含义；
* 输出信息可在原文后添加中文解释；
* 不得为了让示例“看起来完整”而猜测缺失参数。

---

## 9. 百科页面规范

### 9.1 命令参考页面

命令页面优先采用以下结构：

```markdown
# command：中文名称

## 概述
## 官方定义
## 适用版本
## 语法
## 参数说明
## 默认值与单位
## 物理含义
## 最小示例
## 完整示例
## 输出结果
## 常见错误
## 版本差异
## 相关命令
## 官方来源
## 验证状态
```

不适用的小节可以省略，不得为了满足模板而编造内容。

### 9.2 工作流页面

工作流页面优先采用：

```markdown
# 工作流名称

## 适用场景
## 前置条件
## 输入文件
## 操作步骤
## 完整脚本
## 参数选择依据
## 预期输出
## 结果检查
## 常见错误
## 续算与恢复
## 官方来源
## 验证状态
```

### 9.3 错误排查页面

错误条目优先采用：

```markdown
# 错误信息或现象

## 错误表现
## 可能原因
## 检查方法
## 解决方法
## 错误示例
## 修正示例
## 适用版本
## 官方来源
## 验证状态
```

### 9.4 概念页面

物理或模拟概念页面优先采用：

```markdown
# 概念名称

## 定义
## 物理意义
## 数学表达
## 在 EMC/LAMMPS 中的体现
## 常见误解
## 相关概念
## 参考来源
```

---

## 10. 示例规范

所有示例必须标记类型：

| 标记   | 含义         |
| ---- | ---------- |
| 语法示例 | 仅展示命令写法    |
| 最小示例 | 目标是最小可运行   |
| 官方示例 | 来自官方资料     |
| 本机验证 | 已实际执行并保存结果 |
| 待验证  | 尚未实际运行     |

示例必须检查：

1. `units` 与参数单位是否一致；
2. `atom_style` 是否与 data 文件兼容；
3. 力场样式和系数是否完整；
4. 命令 ID 是否重复；
5. 命令执行顺序是否正确；
6. 引用的变量、group、region 和文件是否存在；
7. 温度、压力、阻尼和时间步设置是否合理；
8. 是否依赖用户个人绝对路径；
9. 是否能够说明输入和预期输出；
10. 是否注明适用软件版本。

个人路径，例如：

```text
/opt/emc-9.4.4/
/home/lyd/
/opt/homebrew/
```

只能作为验证环境记录，不能当作所有用户通用路径。

---

## 11. 文档与目录结构

项目采用：

```text
EMC_LAMMPS_Chinese_Encyclopedia/
├── PROJECT.md
├── AGENTS.md
├── TASKS.md
├── REVIEW.md
├── README.md
├── STATUS.md
├── CHANGELOG.md
├── mkdocs.yml
├── docs/
│   ├── 00_navigation/
│   ├── 01_emc_official_translation/
│   ├── 02_emc_setup_reference/
│   ├── 03_emc_command_reference/
│   ├── 04_emc_modeling/
│   ├── 05_force_fields/
│   ├── 06_lammps_user_guide_translation/
│   ├── 07_lammps_command_reference/
│   ├── 08_lammps_file_formats/
│   ├── 09_emc_to_lammps/
│   ├── 10_simulation_workflows/
│   ├── 11_parallel_and_hpc/
│   ├── 12_visualization/
│   ├── 13_analysis/
│   ├── 14_examples/
│   ├── 15_errors/
│   ├── 16_glossary/
│   └── 17_references/
├── examples/
├── scripts/
├── tests/
├── reports/
├── sources/
└── build/
```

不得随意改变已有目录编号和主要分类。

---

## 12. 导航与索引

项目至少维护：

* 使用指南；
* 任务索引；
* 命令索引；
* 关键词索引；
* 错误索引；
* 文件格式索引；
* 力场索引；
* 中英文术语表；
* 参考来源索引。

能够自动生成的索引应使用脚本生成，避免手动内容与实际页面不一致。

新增或移动页面时必须同步检查：

* `mkdocs.yml`；
* Markdown 内部链接；
* 相关页面；
* 索引；
* `STATUS.md`。

---

## 13. 工程质量要求

项目采用 Harness Engineering 思路，通过自动化检查约束 AI 生成内容。

至少应建立：

1. MkDocs 导航文件存在性检查；
2. Markdown 内部链接检查；
3. 页面来源元数据检查；
4. 页面标题重复检查；
5. 术语一致性检查；
6. 示例验证状态检查；
7. `STATUS.md` 页面数量检查；
8. Markdown 代码块闭合检查；
9. MkDocs 严格构建；
10. 必要的 pytest 测试。

推荐验证命令：

```bash
pytest
python scripts/check_links.py
python scripts/check_nav.py
python scripts/check_sources.py
python scripts/check_status.py
mkdocs build --strict
```

只有命令实际成功执行，才能记录为通过。

不得通过删除测试、降低严格模式、忽略错误或删除内容来伪造通过。

---

## 14. 完成状态

任务状态统一使用：

* `待规划`
* `待实现`
* `实现中`
* `等待验收`
* `需要修改`
* `验收通过`
* `阻塞`

Codex完成实现后只能标记为“等待验收”。

只有负责验收的 Claude Code 或用户可以标记为“验收通过”。

---

## 15. 任务优先级

| 优先级 | 含义                      |
| --- | ----------------------- |
| P0  | 网站无法构建、严重链接错误、文件缺失、数据损坏 |
| P1  | 技术事实错误、来源错误、版本混用、虚假验证   |
| P2  | 测试缺失、索引错误、状态统计不准确       |
| P3  | 缺少重要章节、命令、教程或错误条目       |
| P4  | 排版、样式、措辞和一般体验改进         |

必须优先解决高优先级问题。

在工程基础不稳定时，不应优先批量生成新页面。

---

## 16. Agent 协作模式

### 16.1 Claude Code

Claude Code担任：

* 项目规划者；
* 任务拆分者；
* 质量检查者；
* 验收者。

主要负责：

1. 审计项目状态；
2. 编写 `TASKS.md`；
3. 定义验收标准；
4. 对 Codex 的提交进行只读检查；
5. 将问题写入 `REVIEW.md`；
6. 判断任务是否验收通过。

Claude Code原则上不直接大规模修改实现文件。

### 16.2 Codex

Codex担任：

* 项目实现者；
* 自动化检查实现者；
* 文档和代码修改者。

主要负责：

1. 读取任务和审查意见；
2. 实现任务；
3. 编写必要测试；
4. 运行验证命令；
5. 修复实际发现的问题；
6. 更新实现记录、状态和变更日志；
7. 创建范围清晰的 Git 提交。

Codex不能自行宣布验收通过。

### 16.3 用户

用户负责：

* 决定项目总体方向；
* 确定是否扩大项目范围；
* 决定重要版本和内容取舍；
* 处理 Agent 无法自行确定的关键问题；
* 决定是否发布或对外共享。

---

## 17. Git 工作规则

每个任务应对应范围明确的提交。

推荐提交格式：

```text
docs: translate EMC manual chapter 4
fix: repair broken MkDocs links
test: add source metadata validation
feat: generate command index automatically
chore: update project status
```

规则：

1. 修改前检查 Git 状态；
2. 不覆盖用户未提交的修改；
3. 不将无关修改混入同一提交；
4. 不提交 API Key、密码、Cookie 或服务器凭据；
5. 不修改官方原始资料；
6. 验收应针对确定的 commit；
7. 返工应创建新的提交，不修改已经验收的历史；
8. 构建产物是否提交由项目 `.gitignore` 统一决定。

---

## 18. 版权与合规

* 原始文档版权归相应作者和项目所有；
* 中文翻译不是 EMC 或 LAMMPS 官方中文版本；
* 必须保留原作者、软件名称、官方链接和许可证信息；
* 不得移除官方版权声明；
* 不绕过登录、验证码、付费墙或访问控制；
* 不抓取或保存账号凭据和敏感信息；
* 对外发布前必须核查原始文档许可证；
* 在许可证范围不明确时，默认仅用于个人学习、实验室内部和科研查阅。

---

## 19. 阶段性路线

### 阶段 1：项目审计与 Harness 建设

* 修复导航和内部链接；
* 核对真实完成状态；
* 统一版本信息；
* 建立来源检查；
* 建立自动测试；
* 保证 MkDocs 可以严格构建。

### 阶段 2：术语与来源体系

* 建立中英文术语表；
* 统一页面来源元数据；
* 建立来源索引；
* 建立术语一致性检查；
* 区分官方内容、编者注和验证内容。

### 阶段 3：EMC 内容完善

* EMC 官方手册逐章翻译；
* EMC Setup 参考；
* EMC 命令和文件格式；
* EMC 建模教程；
* EMC 力场参考。

### 阶段 4：LAMMPS 内容完善

* 用户指南翻译；
* 常用命令参考；
* 文件格式；
* 错误排查；
* 版本差异。

### 阶段 5：实践与验证

* EMC→LAMMPS 工作流；
* 最小化、升温、NVT、NPT和生产模拟；
* 示例实际运行；
* 保存验证环境和结果；
* 分析与可视化。

### 阶段 6：百科与发布质量

* 自动索引；
* 全文搜索；
* 交叉链接；
* 完整引用；
* 网站可用性检查；
* 版权和许可证检查；
* 离线网站构建。

---

## 20. 项目完成条件

只有满足以下条件，项目才能被视为达到可发布状态：

1. MkDocs 严格构建通过；
2. 内部链接检查通过；
3. 导航不存在缺失页面；
4. 重要页面具有官方来源；
5. 版本信息明确；
6. 术语基本统一；
7. 示例验证状态真实；
8. 不存在已知的严重技术错误；
9. `STATUS.md` 与实际内容一致；
10. 重要 EMC 和 LAMMPS 模块达到计划覆盖率；
11. 版权与许可证已经核查；
12. 网站可以在目标环境离线打开；
13. 已知限制已经公开记录。

“生成了很多页面”不等于项目完成。项目完成必须以内容可靠、来源可查、构建通过和状态真实为依据。

---

## 21. 当前阶段

当前默认阶段为：

> **阶段1：项目审计与 Harness 建设**

在该阶段完成并验收之前，不应进行大规模批量翻译。

下一步应由 Claude Code审计项目并在 `TASKS.md` 中生成第一批 P0～P2 任务，再由 Codex逐项实现。

```
```
